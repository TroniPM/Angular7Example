import { CamIdAgmMarkerDirective } from './cam-id-agm-marker.directive';

describe('CamIdAgmMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new CamIdAgmMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
